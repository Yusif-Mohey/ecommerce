import 'package:flutter/material.dart';

BoxShadow kItemShadow = BoxShadow(
    color: Colors.black.withOpacity(0.2),
    offset: const Offset(0, 10.0),
    spreadRadius: 0.1,
    blurRadius: 13,
    blurStyle: BlurStyle.normal);
